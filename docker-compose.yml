version: "3.8"
services:
  api:
    build: .
    environment:
      DEBUG: "false"
    ports:
        - "8000:8000"
    volumes:
        - "./database/:/home-automation/database/"
        - "./logs/:/home-automation/logs/"
